# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "3.1"
metadata:
  name: bd.businessportal
type: application
server:
  customMiddleware:
    - name: fiori-tools-proxy
      afterMiddleware: compression
      configuration:
        ignoreCertError: false # If set to true, certificate errors will be ignored. E.g. self-signed certificates will be accepted
        ui5:
            path:
            - /resources
            - /test-resources
            url: https://ui5.sap.com
        backend:
          - path: /odata/v4/
            url: https://port4004-workspaces-ws-ycocl.us10.trial.applicationstudio.cloud.sap
    - name: fiori-tools-appreload
      afterMiddleware: compression
      configuration:
        port: 35729
        path: webapp
        delay: 300
    - name: fiori-tools-preview
      afterMiddleware: fiori-tools-appreload
      configuration:
        flp:
          theme: sap_horizon
    # - name: ui5-middleware-simpleproxy
    #   afterMiddleware: compression
    #   mountPath: /odata/v4/
    #   configuration:
    #     baseUri: "https://port4004-workspaces-ws-ycocl.us10.trial.applicationstudio.cloud.sap"
